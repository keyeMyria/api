language: python
python:
  - "3.5"
  - "nightly" # currently points to 3.7-dev
matrix:
  allow_failures:
    - python: nightly

# install: "pip install -r requirements.txt"
# command to run tests
# script: nosetests
notifications:
  email: false
